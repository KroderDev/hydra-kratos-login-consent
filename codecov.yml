coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 0%
        informational: true
    patch:
      default:
        target: 100%
        threshold: 0%

ignore:
  - "realstack/**"
